indicator:
  timeout: 10000
  overlay:
    blur: true
    dim_alpha: 0.6
  image:
    type: default

scopes:
  include:
    - package: com.jini.testapp.scenarios
  exclude:
    # TODO: compile-time ASM scope exclusion 미구현
    # isInstrumentable()에서 YAML exclude 미참조 — ExcludeScenario도 주입됨
    - class: com.jini.testapp.scenarios.ExcludeScenario
